<%= turbo_frame_tag(:menu, style: "display: grid; grid-template-columns: 1fr 2fr") do %>
  <%= render @basket %>

  <%= turbo_frame_tag(:categories, src: basket_category_path(@basket, @default_category)) do %>
    loading...
  <% end %>
<% end %>